[问题]
1. 自我介绍与岗位匹配
    1. 先做一个自我介绍，说明你的 Go 后端经验、性能调优、跨端协同和 AI 工作流。
    2. 是否了解对方正在开发的高端 NAS 产品，和你当前 NAS 项目的匹配点是什么。
2. 文档中心与搜索引擎选型
    1. 智能文档中心为什么从 ES 迁移到 Bleve。
    2. ES 和 Bleve 的应用场景有什么区别，为什么在 NAS 资源受限场景下 Bleve 更合适。
    3. 你对 ES 了解多少，ES 和 Bleve 都使用什么索引机制。
    4. 迁移到 Bleve 之前有没有做过技术决策，除 Bleve 外还了解过哪些搜索或全文检索方案。
    5. Meilisearch、Postgres、SQLite FTS 等方案为什么没有被选择。
3. 文档处理 Pipeline
    1. PDF、Word 等文件内容抽取、分片、入库到搜索引擎的 Pipeline 是怎么设计的。
    2. 如何判断一个文件是否需要重新处理，size、modify time、数据库属性和真实文件属性如何比对。
    3. MIME type 和文件后缀在文档类型识别里分别起什么作用。
    4. PDF、docx、doc 分别用了哪些第三方库或工具做文本提取。
    5. 文本超过 4MB 时如何分块写入 Bleve。
    6. 目录中文档很多时，Pipeline 不是线性处理的话如何加速。
    7. 如何按文件大小、文档类型优先级排序，让用户更快看到可搜索结果。
    8. 文档解析工具调研时对比了哪些工具，评估指标有哪些。
    9. 解析效果是人工抽样还是自动化评估，是否可以引入 LLM judge 做批量评估。
    10. 智能文档中心上线后是否有用户反馈指标、metrics 或埋点。
    11. 处理过什么用户反馈的 bug，当时如何复现、查看日志、定位 CPU/内存问题并解决。
4. Bleve 内存问题与性能优化
    1. 文档中心执行异常时，如何通过 top、pprof 定位到 Bleve 建索引阶段。
    2. 如何结合 AI、技术文档和源码阅读给出优化方案。
    3. Store=false、分块索引、批量提交分别解决了什么问题。
5. NAS 穿透、STUN/TURN 与证书
    1. 异地穿透用的是 WireGuard 还是 STUN/TURN。
    2. STUN 穿透的核心流程是什么，公网地址、信令服务器、NAT 映射表分别承担什么角色。
    3. STUN 穿透是否属于 P2P 协议。
    4. STUN 失败时为什么需要 TURN，TURN 如何解决对称型 NAT 的端口映射问题。
    5. 网络从办公室 Wi-Fi 切换到手机蜂窝网络时，连接是否能迁移或重连。
    6. 穿透链路的传输加密用什么，TLS 证书如何处理。
    7. 是否了解自签证书、Let's Encrypt、openssl 等证书申请方式。
    8. 个人网站 HTTPS 证书是如何配置的，Cloudflare 在其中承担什么角色。
6. AI 相册、人脸识别与相似图检测
    1. AI 相册的人脸识别是怎么做的，DBSCAN 的算法机制是什么。
    2. OpenCV、人脸特征向量、中心特征向量、新照片匹配分别在流程里做什么。
    3. DBSCAN 里的半径和最小点数分别代表什么，核心点、边界点、噪声点如何扩展成簇。
    4. 人脸识别出错后，如何支持用户手动修复、移入新人脸、移出人脸和反馈问题。
    5. 百分之九十七、九十八的识别准确率是基于什么测试集和样本量得到的。
    6. 是否有公司内部测试集，如果要新建一套人脸测试集会怎么做。
    7. pHash 和 qHash 的算法如何实现，如何通过哈希值和汉明距离判断重复照片。
7. 文件同步、fsnotify 与 ulimit
    1. fsnotify 监听机制是什么，它依赖操作系统的哪些文件或目录事件。
    2. 为什么最初用 fsnotify 做文件监听，后来改为 BFS 遍历加队列。
    3. 每个文件夹创建 watcher 时，文件夹数量多会带来什么句柄和内存问题。
    4. 文件数多或操作频繁时，fsnotify 为什么会漏事件。
    5. BFS 遍历加队列如何处理子文件和子文件夹。
    6. 如何调整一个进程可使用的文件句柄数量。
    7. 是否了解 ulimit，ulimit 在 Go + Bleve 场景下有什么帮助。
    8. 在 NAS 资源有限的场景下，soft limit、hard limit 应该设多大，如何通过压测确定。
    9. ulimit 是否越大越好，为什么它和 CPU、内存、应用资源预算存在取舍。
8. MySQL MVCC 与 SQL 调优
    1. MySQL MVCC 是什么，如何在少加锁或不加锁情况下支持并发读写。
    2. undo log 和 ReadView 分别在 MVCC 中起什么作用。
    3. ReadView 中活跃事务 ID 集合、下一个事务 ID、最老事务 ID 分别如何判断可见性。
    4. 生成 ReadView 后是否会出现幻读，这个问题和事务隔离级别有什么关系。
    5. MySQL 默认可重复读级别下，MVCC 和间隙锁如何解决幻读。
    6. 复杂 SQL 如何用 explain 查看性能问题，type、key、possible keys、extra 分别看什么。
    7. type=all、index、range、ref 分别代表什么性能特征。
    8. 当优化器没有选择预期索引时，如何分析原因，是否了解 force index。
    9. 复杂 SQL 是否应该拆开执行，什么时候拆开比 join 更合适。
9. K8s、配置管理与 DevOps
    1. 是否了解 K8s，是否在项目中实际使用过。
    2. K8s 里面如何做配置管理。
    3. etcd 在 K8s 配置管理中承担什么角色。
    4. 更新一份配置后，其他服务如何获得这个更新。
    5. 当前项目使用什么配置中心，是否有统一配置管理和下发配置。
    6. 云端部分是否只负责信令交换，是否涉及更多云端功能。
    7. 你看新机会的原因是什么，想在哪些技术栈上成长。
    8. 对 DevOps、K8s、云原生、SRE、AI Agent 的兴趣分别是什么。
    9. 如果岗位中 30% 时间做 K8s 管理和 CI/CD 集成，其他时间做 NAS 后端重构，是否感兴趣。
10. 反问、可观测性与面试反馈
    1. 如何为 Bleve 搜索或文档中心获得真实用户反馈。
    2. 错误跟踪、错误上报、OpenTelemetry、客户端埋点和问题上报入口分别能提供什么反馈。
    3. Prometheus、Grafana、Loki、OpenTelemetry 在云端可观测性中如何使用，资源占用如何评估。
    4. OpenTelemetry 为什么成为可观测性标准，和 Jaeger 的关系是什么。
    5. 对方团队成立多久，母公司是什么。
    6. 进入下一轮面试的期待程度是多少。
    7. 简历上写的技术点是否扎实，后续需要如何复盘和补充。
    8. 如果面试通过，多久可以到岗，是否需要交接文档。

Bleve、ES、文档 Pipeline、STUN、TURN、TLS、DBSCAN、pHash、fsnotify、ulimit、MVCC、Explain、K8s、DevOps、OpenTelemetry


[同类追问预测]
这个面试官的风格是从简历项目一路深挖到真实工程边界：先问“为什么选这个方案”，再问“有没有比较过其他方案”，接着压到“上线后怎么评估、怎么压测、怎么处理用户反馈”。他也会现场让候选人用 AI 学一个不熟悉的问题，观察信息检索、提问上下文补充和复述能力。后续很可能继续围绕 NAS 资源约束、文档搜索、可观测性、K8s/DevOps 和数据库调优追问，并且会把问题落到可执行的指标、压测和取舍上。

1. 文档中心与搜索引擎
    1. Bleve 的倒排索引是如何组织 term、posting list 和 document id 的。
    2. Bleve 和 ES 在索引构建、查询、分片、持久化和资源占用上有什么本质差异。
    3. SQLite FTS5、Postgres tsvector、Meilisearch、Bleve 分别适合什么规模和部署形态。
    4. 4MB 分块后，搜索结果如何合并、排序和高亮。
    5. 分块索引是否会影响召回率，如何验证不会漏搜。
    6. 如果用户上传超大 PDF、加密 PDF、扫描版 PDF 或乱码文档，Pipeline 如何兜底。
    7. 文档解析失败后任务状态如何记录，是否需要重试、跳过或告警。
    8. 在 NAS 上建立全文索引时，如何限制 CPU、内存、磁盘 IO 和索引文件体积。
2. Pipeline 评估与用户反馈
    1. 如何设计一套自动化 benchmark 来评估文档解析工具的准确率、耗时和资源占用。
    2. 如果引入 LLM judge，如何设计评分维度、样本集和一致性校验。
    3. 用户搜索“合同金额”搜不到时，你如何判断是解析问题、索引问题还是分词问题。
    4. 文档中心上线后需要埋哪些指标来判断用户是否真的在使用。
    5. 搜索点击率、无结果率、索引失败率、索引耗时、内存峰值分别如何采集。
    6. 资源有限的 NAS 上如何部署 Prometheus、OpenTelemetry 或轻量级自研埋点。
3. 穿透、证书与网络
    1. STUN 返回的公网地址为什么在对称型 NAT 下不能直接复用。
    2. TURN 中继的带宽成本、延迟和可靠性如何评估。
    3. 信令服务器挂了以后，已有连接和新连接分别会受到什么影响。
    4. 网络切换后的重连如何触发，连接状态和 session 如何恢复。
    5. TLS 握手过程中证书验证、密钥协商和数据加密分别发生在什么时候。
    6. Let's Encrypt 的 ACME 协议大致如何完成域名验证和证书签发。
    7. 自签证书在内网设备、NAS 设备和移动端访问中会遇到什么信任问题。
4. AI 相册与算法评估
    1. DBSCAN 的 eps 和 minPts 如何选择，参数变化会如何影响聚类结果。
    2. 人脸特征向量的相似度一般用欧氏距离还是余弦相似度，阈值如何确定。
    3. 新照片加入已有簇时，中心向量是重新计算还是固定不变。
    4. 用户手动合并或拆分人脸后，算法数据和用户修正数据如何保持一致。
    5. 公司内部人脸测试集如何采集、脱敏、标注和分层。
    6. pHash、aHash、dHash 在抗缩放、抗裁剪、抗亮度变化上有什么差异。
    7. 相似图检测误删风险如何控制，是否需要二次确认和回收站。
5. 文件同步与资源限制
    1. fsnotify 在 Linux、macOS、Windows 底层分别依赖什么机制。
    2. inotify 的 watch 数量和 file descriptor 数量分别受哪些系统参数限制。
    3. ulimit -n、fs.inotify.max_user_watches、max_user_instances 有什么区别。
    4. BFS 全盘扫描如何控制 IO，避免影响用户正常访问 NAS。
    5. 如果扫描过程中用户继续新增、删除、移动文件，如何保证最终一致。
    6. 队列任务如何去重，如何记录 pending、indexing、failed、done 状态。
    7. 压测 ulimit 时，如何设计目录数量、文件数量、并发搜索和索引写入组合。
6. MySQL 与 SQL
    1. ReadView 的可见性判断规则能不能用具体事务 id 举例说明。
    2. 快照读和当前读分别是什么，select、select for update、update 分别属于哪类。
    3. 间隙锁和 next-key lock 在什么条件下生效。
    4. 可重复读下为什么普通快照读不产生幻读，而当前读需要锁来避免幻读。
    5. explain analyze 和普通 explain 有什么区别。
    6. possible_keys 有索引但 key 没选中时，可能是统计信息、选择度、函数、隐式转换还是排序导致。
    7. force index、use index、ignore index 分别有什么差异和风险。
    8. 拆 SQL 后如何处理事务一致性、网络往返和临时结果集大小。
7. K8s、DevOps 与配置
    1. ConfigMap 和 Secret 有什么区别，适合存什么配置。
    2. Pod 如何通过环境变量、volume mount 或 API watch 获取配置变化。
    3. 配置变更后为什么有些应用需要重启，有些可以热加载。
    4. etcd 为什么需要强一致性，Raft 在里面解决什么问题。
    5. Deployment、StatefulSet、DaemonSet 分别适合什么工作负载。
    6. CI/CD 流程里如何做镜像构建、版本标记、灰度发布和回滚。
    7. DevOps 和 SRE 的职责边界是什么，如何避免只变成运维执行。
8. AI 工具使用与现场学习
    1. 面试现场用 AI 查资料时，如何先给 AI 补充业务上下文和资源限制。
    2. AI 给出一个参数值后，如何验证它不是拍脑袋。
    3. 什么时候需要让 AI 给命令示例，什么时候需要让 AI 给实验方案。
    4. 如何把 AI 的回答转化为自己的工程判断，而不是直接复读。
    5. 如果 AI 答案和你的项目经验冲突，你会怎么确认。
    6. 工作中如何沉淀 prompt、脚本、benchmark 和复盘文档。
9. 岗位匹配与反问
    1. 如果岗位同时包含 K8s、CI/CD、NAS 后端重构和 AI Agent，你如何排序学习优先级。
    2. 你希望一年后在云原生、后端架构、底层性能还是 AI 应用上达到什么能力。
    3. 面对 30% DevOps 和 70% 后端重构的工作结构，你如何判断是否符合个人目标。
    4. 如果重构现有 NAS 后端，你会先看哪些模块、指标和历史问题。
    5. 你会如何向面试官反问团队当前最大技术债、上线节奏、可观测性和故障响应方式。
    6. 简历上每个技术点如何准备“做过什么、为什么做、效果如何、踩过什么坑、还能怎么优化”的闭环。
