[问题]
1. Go Channel 底层与使用
    1. Channel 的底层结构包含哪些核心字段和队列。
    2. Channel 发送和接收的整体工作流是什么，阻塞、缓冲区、等待队列分别在什么时候参与。
    3. Channel 有哪些典型使用场景，比如生产者消费者、并发控制、超时取消。
2. Go Map 底层与并发安全
    1. Go Map 的底层结构是什么，Go 1.24 前后的结构变化是什么。
    2. SwissTable 的组织方式和 SIMD 优势是什么。
    3. 为什么 Go 官方没有把普通 Map 设计成天然支持并发读写。
    4. 并发读写 Map 时除了锁竞争，还有哪些数据一致性或结构变化风险。
3. NAS 项目并发处理
    1. NAS 云存储项目在多用户或恶意请求并发场景下怎么处理。
    2. 项目里如何使用限流、缓存和锁来控制并发。
    3. 使用 `sync.Mutex` 时，如何保证同一类操作不能被多个协程同时执行。
    4. 是否了解自旋锁、`atomic` 原子操作，以及 Redis `setNX` + EX 这类分布式锁方案。
4. AI 辅助开发工作流
    1. 你现在是否使用标准化的 AI 开发流程，例如先讨论需求、拆 Task、再做 TDD。
    2. SuperPervos 插件在你的 Codex 工作流里怎么使用。
    3. TDD 里先写测试的目的是什么，为什么不是“写错误的测试”。
    4. 你是否会对 AI 工作过程做复盘，如何沉淀规则到 AGENTS.md。
    5. AGENTS.md 和 SQ 的区别是什么，分别在什么时机加载或触发。
5. 算法题：反转链表
    1. 现场实现反转链表的主体函数。
    2. 用测试用例验证反转链表实现。
    3. 代码里 header/head 的定义和传参方式是否正确，是否应该作为全局变量。
    4. 解释递归实现反转链表的终止条件和递推过程。
6. 反问与团队情况
    1. 英国产品为什么把技术团队设在中国。
    2. 中国团队主要负责哪些模块。

Channel、Map、并发控制、Mutex、atomic、分布式锁、AI 工作流、TDD、反转链表


[同类追问预测]
这个面试官的风格是先从简历和项目里提到的关键词切入，再追问底层结构、真实使用场景和现场解释能力；遇到回答不完整时，会补一个更贴近业务或更底层的概念继续试探。后续很可能继续围绕 Go 并发原语、项目里的锁和限流是否真实落地、AI 工作流是否讲得清楚、以及算法代码能否自洽来追问。

1. Go Channel 底层与使用
    1. 往已关闭的 Channel 发送数据、从已关闭的 Channel 接收数据分别会发生什么。
    2. 无缓冲 Channel 和有缓冲 Channel 的发送接收路径有什么不同。
    3. `select` 里多个 Channel 同时 ready 时如何选择，`default` 分支什么时候执行。
    4. Channel 为什么需要发送等待队列和接收等待队列，队列里存的是什么。
    5. 用 Channel 做并发控制时，和 `sync.WaitGroup`、`context.Context` 分别适合解决什么问题。
2. Go Map 底层与并发安全
    1. Go 1.24 的 SwissTable 为什么能提升查找效率，control bytes 大致起什么作用。
    2. Map 扩容或迁移过程中，如果并发读写会产生什么风险。
    3. `sync.Map` 适合读多写少还是写多读少，为什么普通业务不一定优先用它。
    4. 用 `RWMutex` 包 Map 和用 `sync.Map` 相比，在性能、类型安全、代码复杂度上有什么取舍。
    5. 如果项目里某个 Map 只在初始化后读，是否还需要加锁，如何证明它是安全的。
3. NAS 项目并发控制
    1. 你说的限流是按用户、IP、设备还是接口维度做的，限流 key 怎么设计。
    2. Rate 限流超限后返回什么错误，前端或调用方如何感知。
    3. Ristretto 缓存的 key、value、TTL 怎么设计，缓存命中后是否会带来数据过期问题。
    4. 某个 NAS 操作加锁后，如果执行过程中失败或 panic，锁如何释放。
    5. 单机锁只能控制单进程内并发，如果部署多实例或多设备协作，怎么升级成分布式锁。
    6. Redis `setNX` + EX 分布式锁如何避免死锁、误删别人的锁和锁过期但业务没执行完的问题。
4. atomic、自旋锁与锁选择
    1. `atomic.CompareAndSwap` 适合解决什么问题，和 `Mutex` 的使用边界是什么。
    2. 自旋锁为什么不能一直自旋，什么场景下自旋比阻塞更合适。
    3. Go 的 `sync.Mutex` 内部是否有自旋，什么情况下会进入饥饿模式。
    4. 如果一个操作耗时较长，为什么不适合用自旋锁。
    5. 如何用 atomic 实现一个简单的状态机，避免重复执行同一个任务。
5. AI 辅助开发工作流
    1. 你如何把一个需求拆成适合 AI 执行的小 Task，怎么判断拆得太粗还是太细。
    2. AI 先写测试时，如何保证测试不是为了迎合实现而写。
    3. 如果 AI 生成的测试本身有问题，你如何发现并修正。
    4. AGENTS.md 里适合沉淀哪些规则，哪些规则不应该长期写进去。
    5. SQ 按需加载时，如何避免上下文过多导致模型忽略关键约束。
    6. 你如何评估一次 AI 工作流是否真的提升效率，而不是增加沟通成本。
6. 算法题：反转链表
    1. 反转链表的迭代写法是什么，时间复杂度和空间复杂度分别是多少。
    2. 递归写法为什么需要返回新的头节点，原 head 的 next 应该如何处理。
    3. 空链表、单节点链表、两个节点链表分别怎么处理。
    4. 如果链表有环，反转链表会发生什么，如何提前检测。
    5. 现场写代码时如何先定义结构体、构造测试数据，再验证输出结果。
