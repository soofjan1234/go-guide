[问题]
1. Go Channel 底层与使用
    1. Channel 的底层结构包含哪些核心字段和队列。
    2. Channel 发送和接收的整体工作流是什么，阻塞、缓冲区、等待队列分别在什么时候参与。
    3. Channel 有哪些典型使用场景，比如生产者消费者、并发控制、超时取消。
2. Go Map 底层与并发安全
    1. Go Map 的底层结构是什么，Go 1.24 前后的结构变化是什么。
    2. 为什么 Go 官方没有把普通 Map 设计成天然支持并发读写。
3. NAS 项目并发处理
    1. NAS 云存储项目在多用户或恶意请求并发场景下怎么处理。
    2. 项目里如何使用限流、缓存和锁来控制并发。
    3. 是否了解自旋锁、`atomic` 原子操作，以及 Redis `setNX` + EX 这类分布式锁方案。
4. AI 辅助开发工作流
    1. 你现在是否使用标准化的 AI 开发流程，例如先讨论需求、拆 Task、再做 TDD。
    2. SuperPervos 插件在你的 Codex 工作流里怎么使用。
    3. TDD 里先写测试的目的是什么，为什么不是“写错误的测试”。
    4. 你是否会对 AI 工作过程做复盘，如何沉淀规则到 AGENTS.md。
    5. AGENTS.md 和 SQ 的区别是什么，分别在什么时机加载或触发。
5. 算法题：反转链表
    1. 现场实现反转链表的主体函数。
    2. 用测试用例验证反转链表实现。
    3. 代码里 header/head 的定义和传参方式是否正确，是否应该作为全局变量。
    4. 解释递归实现反转链表的终止条件和递推过程。
6. 反问与团队情况
    1. 英国产品为什么把技术团队设在中国。
    2. 中国团队主要负责哪些模块。

Channel、Map、并发控制、Mutex、atomic、分布式锁、AI 工作流、TDD、反转链表


[同类追问预测]
这个面试官的风格是先从简历和项目里提到的关键词切入，再追问底层结构、真实使用场景和现场解释能力；遇到回答不完整时，会补一个更贴近业务或更底层的概念继续试探。后续很可能继续围绕 Go 并发原语、项目里的锁和限流是否真实落地、AI 工作流是否讲得清楚、以及算法代码能否自洽来追问。

1. AI 辅助开发工作流
    1. 你如何把一个需求拆成适合 AI 执行的小 Task，怎么判断拆得太粗还是太细。
    2. AI 先写测试时，如何保证测试不是为了迎合实现而写。
    3. 如果 AI 生成的测试本身有问题，你如何发现并修正。
    4. AGENTS.md 里适合沉淀哪些规则，哪些规则不应该长期写进去。
    5. SQ 按需加载时，如何避免上下文过多导致模型忽略关键约束。
    6. 你如何评估一次 AI 工作流是否真的提升效率，而不是增加沟通成本。
