[问题]
1. 自我介绍与项目挑战
    1. 用两分钟以内做一个简短自我介绍。
    2. 过去项目经历里遇到的最大挑战是什么，怎么解决。
    3. 文档中心异常时，你如何确认现象、复现、查看日志、定位 CPU 或内存问题。
    4. 你如何通过 pprof、阅读 Bleve 源码和方案改造降低内存占用。
2. 系统瓶颈与性能优化
    1. NAS 项目里的系统瓶颈一般会出现在哪些方面。
3. Go 基础
    1. Slice 的扩容策略是什么，包括长度、容量阈值、1.25 倍扩容和内存对齐。
    2. 多个 Goroutine 同时读写 Map 会发生什么，`fatal error` 能否 recover。
    3. Map 遍历顺序是否确定。
    4. 值 receiver 和指针 receiver 的区别、适用场景是什么。
    5. defer 的执行顺序是什么。
    6. 是否了解反射，项目里有没有使用过。
    7. 一个包里有多个 init 时，初始化顺序是什么。
    8. 什么是内存逃逸分析，常见逃逸场景有哪些，如何检测。
4. Go 并发
    1. Channel 通过什么数据结构支持有缓冲和无缓冲特性。
    2. 对已经关闭的 Channel 读和写分别会发生什么。
    3. 竞态是什么，什么场景会产生，如何检测。
5. 微服务、RPC 与 Gin
    1. 微服务方面用过哪些框架，是否写过相关小组件。
    2. 是否使用过 Prometheus、Grafana、Loki、Trace 做监控和链路追踪。
    3. 服务链路划分或服务模型设计有没有相关经历。
    4. 是否了解 gRPC 或其他 RPC 框架。
    5. Gin 请求从进入框架到 handler 处理的大致流程是什么。
    6. Gin 的路由匹配结构和参数匹配方式是什么。
    7. 自己实现过哪些中间件，例如鉴权、限流、缓存、锁、跨域。
    8. 框架或服务的优雅关闭是什么，通常怎么支持。
6. API 版本化
    1. 多端正在使用的接口需要做不兼容改动时，API 版本化有哪些方式。
    2. 版本号放在 URL、Header、Media Type 中各有什么适用场景。
7. AI、MCP、Skill 与 RAG
    1. 现在写代码是以 AI 为主还是自己写为主。
    2. 用过哪些 AI 模型和工具。
    3. 是否沉淀过自己的 Skill，或者做过 MCP 工具。
8. 反问与入职信息
    1. 当前是否在职，到岗时间多久。
    2. 公司现在有多少人。

项目挑战、性能优化、Slice、Map、逃逸分析、Channel、Data Race、Gin、API 版本化、AI、MCP、RAG


[同类追问预测]
这个面试官的风格是覆盖面很广，先从项目真实性和性能优化细节切入，再快速扫 Go 基础、并发、框架、微服务和 AI 工程化；如果候选人回答卡顿，会继续换一个更基础或更贴近实战的问题确认边界。后续很可能继续追问“你是否真的做过”“底层机制是否清楚”“工程方案如何落地和取舍”。

1. 项目挑战与性能优化
    1. pprof 里 heap、alloc_space、inuse_space 分别适合看什么。
    2. 你如何判断文档中心是内存泄漏、内存峰值过高，还是 GC 压力过大。
    3. Bleve 建索引时为什么会占用大量内存，分块索引会带来什么副作用。
    4. 并行提交提升吞吐时，如何控制并发数，避免 CPU 或内存进一步升高。
2. Go Slice、Map 与逃逸
    1. Slice 作为函数参数传递时，修改元素和 append 分别会不会影响调用方。
    3. concurrent map read and map write 为什么是 fatal，而不是普通 panic。
    4. 用 `go build -gcflags=-m` 看到某个变量逃逸后，如何判断是否值得优化。
4. AI、MCP、Skill 与 RAG
    1. MCP 工具和普通脚本的区别是什么，为什么要让 AI 通过 MCP 调用能力。
    2. 你写 MCP 工具时如何定义输入输出 schema，如何处理接口调用失败。
    3. embedding 相似度匹配可能召回错误内容时，如何验证和兜底。
    6. AI 生成代码时，你如何通过测试、review 和日志确认它没有引入隐藏问题。
