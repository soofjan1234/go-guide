[问题]
1. Go GMP 调度
    1. 说明 GMP 模型
    2. Goroutine 在阻塞时，G/M/P 分别会发生什么变化。
    3. Goroutine 和操作系统线程有什么区别
2. Go 基础结构
    1. channel 的底层结构
    2. slice 的扩容，和map 有什么不同
    4. Go map 为什么不是并发安全的，并发写会触发什么问题，有哪些常见解决方案。
3. 逃逸分析与性能调优
    1. Go 逃逸分析，举例说明变量为什么会逃逸
    3. 你在文档中心内存占用优化中，是如何一步步定位问题、使用 pprof、阅读 Bleve 源码并最终降低内存占用的。
4. MySQL 与数据库优化
    1. MySQL 的四种事务隔离级别分别是什么。
    2. 结合项目说明你做过的 MySQL 性能调优
5. NSQ 消息队列
    1. NSQ 是否能保证消息不丢失，消息丢失可能发生在哪些环节。
    2. 生产端、NSQ 服务端、消费端分别可以采取哪些策略降低消息丢失风险。
    3. NSQ 里是否有死信队列概念，失败消息通常如何处理。
7. NAS 项目与团队背景
    1. NAS 项目研发了多久，你是在项目早期、中期还是后期加入的。
    2. 项目技术选型由谁主导，你在技术选型中承担什么角色。
    3. 你在团队里的技术水平、职责边界、与 TL/产品经理/其他研发的关系是什么。
8. NAS 发布、运维与安全
    1. NAS 服务如何发布，是通过 Docker 镜像、固件烧录还是 OTA 分发。
    2. 用户 NAS 出现问题时，你们有哪些监控、日志中心或远程排查能力。
    3. 远程穿透查看用户 NAS 日志是否有安全风险，如何限制访问范围和取得用户授权。
9. Docker、Compose
    1. 常用 Docker 命令有哪些，Dockerfile 和 Docker Compose 分别解决什么问题。
    2. docker compose 中服务依赖启动顺序如何处理，比如应用依赖 MySQL/Redis 时如何配置。

闭包、数据库真实优化、死信队列、nas远程排查安全运维、Docker

[同类追问预测]
这个面试官的风格是沿着简历关键词逐层追问，先问底层概念，再要求结合项目举例；如果发现项目经历和简历表述不一致，会持续追问真实性、职责边界和数据规模。后续很可能继续围绕“你是否真正做过”“为什么这样设计”“异常情况怎么兜底”来压细节。

1. Go 基础结构
    1. 关闭 channel 后继续发送、继续接收分别会发生什么。
    3. slice 扩容后原数组什么时候还会被引用，可能造成什么内存问题。
    4. map 扩容和迁移大致怎么做，为什么遍历 map 是无序的。
    5. sync.Map 适合什么读写场景，和 RWMutex 包 map 相比有什么取舍。
3. 逃逸分析与内存优化
    1. 你如何用 `go build -gcflags=-m` 判断某个变量是否逃逸。
    2. pprof 里 heap、alloc_space、inuse_space 分别看什么，定位内存峰值应该看哪个。
    3. 如果线上无法复现内存问题，你会如何采集证据和保护用户数据。
6. NSQ 消息可靠性
    1. NSQ 的 ack、requeue、timeout 机制分别是什么。
    2. 消费者处理失败后，如何控制重试次数和避免无限重试。
    3. 如果没有内置死信队列，你会如何设计失败消息落库或转移 topic。
    4. 消费端如何保证幂等，重复消费会导致什么业务问题。
    5. NSQ 和 Kafka、RabbitMQ 在可靠性、吞吐、运维复杂度上有什么区别。
7. NAS 运维与安全
    1. 远程穿透只允许访问日志服务，技术上如何做权限隔离。
    2. 用户授权邀请码如何生成、过期、撤销和审计。
    3. 日志里如果包含照片路径、用户信息或 token，如何脱敏。
    4. NAS 离线、内网不可达或固件版本不一致时，如何远程排障。
    5. OTA 升级失败如何回滚，如何避免设备变砖。
8. Docker、Compose 与 K8s
    1. Dockerfile 多阶段构建如何减小 Go 服务镜像体积。
    2. docker compose 里 `depends_on` 和 `healthcheck` 分别解决什么问题。
    3. 服务依赖 MySQL/Redis 时，只保证容器启动够不够，如何等待服务真正可用。
    4. 如果没有 K8s 实战，只学习过 Deployment、Service、Ingress，简历应该怎么写更准确。
    5. NAS 固件内运行容器和云端 K8s 运行容器，在资源限制、升级、日志采集上有什么不同。
