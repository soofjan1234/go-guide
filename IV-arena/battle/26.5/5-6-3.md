1. 项目 √
    1. 你主导的 NAS 和传统「穿透」：用户从外网访问家里的 NAS，中间完整链路是什么
    2. 手机与 NAS 在同一局域网下点开启穿透，再到外出后远程访问，流程是什么
    3. 公司里几条业务分别是什么；你本人负责哪些部分、哪些模块是你做的、哪些决策是你做的
    4. 项目里有没有出过比较严重的问题、比较大的故障；最难啃的点是什么
    5. 简历里写的吞吐提升约 1.6 倍、延迟降低约 50%，这些数字是怎么测出来的
2. 系统设计
    1. 是否了解 Clawbot 这类东西
    2. 前提：家庭里的 AI 电脑——上电、用户扫码绑定设备、任意时间地点外网访问设备上的服务（照片、本地 AI、设备状态等）；设备不依赖其他应用、用户也没配路由器。请设计相关系统
    3. 扫码绑定之后，手机如何与这台设备绑定；异地如何远程访问（会用什么：P2P、Relay、Cloudflare Tunnel、自建 Tunnel 等，怎么选、怎么落地）
    4. 私密数据（照片、对话、账号密码等）：如何让一部分可被本地模型学习、一部分不让学；云端应该能看到什么、不应该看到什么
    5. 设备与云端距离很远时，如何加密、如何保证信息不被解析；中间人场景下云端能做什么；加密空间里的数据是否都能走远程传输
    6. 全球大量设备 OTA 推送更新，更新失败导致设备起不来，怎么办
    7. P2P / Relay 以及云端实际职责、防 replay、仅靠 HTTPS 是否足够等延伸讨论
3. 编码题（设备心跳）
    1. 简化版：设备每 10 秒向云端上报状态，云端判断设备是否在线，用户可随时访问云端查询设备在线/离线；写出核心结构体、接口、主要逻辑，以及你怎么测
    2. 上报接口（如 POST）：需要携带哪些字段（如 device id、timestamp、meta、final status 等）
    3. 查询设备在线状态的接口
    4. 心跳每 10 秒一次，若 30 秒内没有收到设备心跳，如何判定
    5. 设备断电时，如何知道它是 offline
    6. 有人伪造 device id 持续伪造心跳，怎么防

