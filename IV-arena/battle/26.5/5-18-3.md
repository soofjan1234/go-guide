[原题]
1. go 
    1. GMP：调度模型、工作窃取、GMP阻塞
    2. Channel：`close` 内部大致做什么？如何避免向已关闭 Channel 发送？
    3. 内存：Go 内存大致分哪几类？
    4. 内存泄漏：常见原因？如何排查？

2. mysql
    1. B+ 树
    2. 锁讲讲
    3. 事务讲讲

3. redis有用到吗

4. 其它项目
    1. 文档中心：为何不用 ES 做全文检索？技术是否对比过其它？为什么内存占用高
    2. 限流缓存：概念，以及如何一步步优化的
    3. 消息队列：如何选型？顺序性如何保证？
    4. 最近又看什么好的技术文章

---

[表现]
1. go
    1. 可以，阻塞再背熟点
    2. 可以
    3. 一般，再背熟点
    4. 可以，再背熟点

2. mysql
    1. 可以
    2. 一般，再背熟点。

---

[答案]
1. 如何避免向已关闭 Channel 发送？

- 只有发送方（生产者）负责 close(ch)，接收方只读，不要关数据 channel。
- 在「确定不会再发送」之后再 close：例如生产协程发完所有任务、消费者都处理完，由生产者 close。


2. nas是什么
我们做的是家用/小团队的私有云存储设备，可以理解成带大硬盘的智能存储盒子：用户把照片、文档放在自己家里或公司局域网里，手机、电脑通过 App 访问，数据在客户自己的机器上，不是部署在我们公司机房的 SaaS 服务器。