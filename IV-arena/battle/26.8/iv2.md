[问题]
1. Go 学习背景、Java 与 Go 对比
    1. Go 是在什么阶段学习和开始使用的。
    2. Java 和 Go 最大的区别是什么。
    3. 线程和协程的区别是什么。
    4. 为什么认为 Go 协程能支撑更高并发，依据是什么。
3. Go GMP 调度模型
    1. GMP 模型是什么，G、M、P 分别代表什么以及三者之间的关系。
    2. P 的本地队列长度为 256，如果本地队列满了会发生什么。
    3. 全局队列是什么结构。
    4. Go 的工作窃取机制是什么，是否了解更底层的实现方式。
    5. Go 调度器寻找可运行 G 的大致流程是什么。
    6. Go 阻塞处理有哪些类型，普通阻塞、网络阻塞、系统调用阻塞分别如何处理。
4. Go 并发实践与协程池
    1. 平时在 Go 中如何做并发处理。
    2. 是否用过协程池
5. Gin、Echo 与 Web 框架
    1. 是否使用过 Gin 框架。
    2. Gin 和 Echo 的优缺点或差异是什么。
    3. Gin / Echo 这类 Web 框架底层基于什么网络机制或网络框架。
6. MySQL 熟悉程度与索引
    1. MySQL、Redis、Kafka 等数据库或中间件中，自己掌握最深的是哪个。
    2. 当前项目使用的 MySQL 版本是什么。
    3. MySQL 联合索引的最左匹配原则是什么。
    4. 哪些场景会导致索引无法使用。
    5. 类型转换在什么情况下会导致索引失效，什么情况下仍然能走索引。
7. MySQL MVCC 与事务隔离级别
    1. MVCC 是什么，中文名是什么，核心解决什么问题。
    2. MVCC 中 undo log 和 ReadView 的作用是什么。
    3. MySQL 的事务隔离级别有哪些，默认隔离级别是什么。
    4. 读已提交和可重复读分别是什么。
    5. MVCC 如何解决不可重复读问题。
8. MySQL B+ 树、回表与覆盖索引
    1. 为什么联合索引会有最左匹配原则。
    2. B+ 树索引是什么，存储结构是什么。
    3. B+ 树一般有几层结构。
    4. `select *` 和 `select` 特定字段在什么情况下查询效率差异最大。
    5. 回表和覆盖索引为什么会影响查询性能，数据量和磁盘页大小对性能有什么影响。

