[问题]
1.  Go 并发实践与协程池
    1. 平时在 Go 中如何做并发处理。
    2. 是否用过协程池
5. Gin、Echo 与 Web 框架的区别
6. MySQL 熟悉程度与索引
    1. MySQL 联合索引的最左匹配原则是什么。
    4. 哪些场景会导致索引无法使用。
    5. 类型转换在什么情况下会导致索引失效，什么情况下仍然能走索引。
7. MySQL MVCC 与事务隔离级别
    1. MVCC 中 undo log 和 ReadView 的作用是什么。
    3. 读已提交和可重复读分别是什么。
    5. MVCC 如何解决不可重复读问题。
8. MySQL B+ 树、回表与覆盖索引
    1. 为什么联合索引会有最左匹配原则。
    2. `select *` 和 `select` 特定字段在什么情况下查询效率差异最大。

