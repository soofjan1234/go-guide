[问题]
1. 开场、自我介绍与求职动机
    1. 请做一下自我介绍。
    2. 目前仍在职，为什么考虑换公司。
    3. 如何看待自己的职业规划；是否只要岗位满足高并发、分布式方向，就愿意在该领域长期发展。
    4. 自己的技术兴趣在哪一块。
    5. 是否希望继续找后端岗位。
    6. 目前在广州还是深圳；是哪里人；如何看待去杭州发展。
2. Go 切片、数组与扩容
    1. Go 的切片和数组有什么区别，包括长度、底层结构和作为函数参数传递时的差异。
    2. append 切片时会发生什么；切片扩容策略和实际内存分配行为是什么。
3. Go Map、Channel 与 GMP
    1. Go 1.24 之后 Map 的 Swiss Table 底层结构是什么，如何定位槽位，SIMD 有什么作用。
    2. Channel 的底层结构是什么；发送数据时如何处理关闭、等待接收、缓冲区可用、非阻塞和阻塞等情况。
    3. Go 的 GMP 模型是什么；G、M、P 和全局调度器各自包含什么，它们之间是什么关系。
    4. Go 调度从程序入口、初始化、创建 G 到 findRunnable 循环的大致流程是什么。
4. Go 内存管理、逃逸、泄漏与 GC
    1. Go 的内存管理机制是什么；mcache、mcentral、mheap、span 和页分配分别有什么作用。
    2. 内存逃逸和内存泄漏有什么区别；常见原因分别是什么；如何排查。
    3. Go GC 有哪些触发时机，垃圾回收主要分为哪些阶段。
    4. 三色标记中的白、灰、黑对象如何表示和流转。
    5. 并发标记可能出现什么错误清理问题，写屏障如何避免该问题。
5. Go 线上问题与 pprof
    1. 是否排查过 Go 线上问题；文档中心内存异常是如何复现、定位和解决的。
    2. pprof 如何查看 CPU 和内存问题；flat、cum、alloc 和 inuse 分别表示什么。
6. MySQL 事务、日志与 MVCC
    1. MySQL 事务的 ACID 特性分别是什么。
    2. 原子性、隔离性和持久性分别如何通过 undo log、MVCC、锁和 redo log 实现。
    3. undo log 的版本链和 row pointer 如何支持回退。
    4. redo log 记录什么，循环写和 checkpoint 如何支持崩溃恢复。
    5. MVCC 如何结合 undo log 和 Read View 判断某个版本是否可见。
7. Redis 缓存问题与数据一致性
    1. 缓存穿透、缓存击穿和缓存雪崩有什么区别，分别如何处理。
    2. 布隆过滤器由什么组成，如何判断元素一定不存在或可能存在，为什么不能直接删除元素。
    3. MySQL 和 Redis 同时存储数据时，如何通过旁路缓存保证一致性。
    4. 先删除缓存再更新数据库，以及同时更新数据库和缓存，分别可能出现什么不一致问题。
8. 计算机网络、DNS 与 HTTPS
    1. 计算机网络有哪几层，各层常见协议或介质是什么。
    2. 在浏览器输入网址后，从 DNS 解析、TCP/TLS 建连、逐层封装、路由转发到服务端响应和页面解析，会经历哪些流程。
    3. HTTP 和 HTTPS 有什么区别；TLS 的密钥协商和加密过程是什么。
    4. 中间人攻击是怎么发生的；数字证书和数字签名如何验证通信方身份。
9. 最艰难的任务、文档中心与 AI 相册
    1. 工作几年中最艰难的任务是什么，为什么认为文档中心和 AI 相册比较困难。
    2. 文档中心的使用场景是什么；它是否作为用户 NAS 系统中的功能运行。
    3. NAS 是软硬件一体化交付，还是只销售软件。
    4. NAS 一体机使用什么芯片架构，CPU、内存和 AI 加速规格如何。
10. 工作时间、加班与反问
    1. 目前一般的上下班时间是什么样的。
    2. 是否能够接受 996；如何看待项目赶期和长期加班。
    3. 候选人还有什么想了解的问题。
