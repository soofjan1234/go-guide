[问题]
1. Go 
    1. Go 发起 HTTP 请求等待网络 I/O 时，阻塞的是 goroutine 还是 OS 线程
    2. Go 为什么需要逃逸分析、编译器如何决定变量分配在栈或堆，以及如何查看逃逸分析结果
2. MySQL 
    1. undo log、redo log、binlog 写入顺序，redo log 与 binlog 如何完成两阶段提交
    2. lsn是什么
    3. redolog可以做主从同步吗

网络阻塞、mySQL日志