[问题]
1. 网络协议、HTTP/HTTPS 与 TCP
    1. 浏览器输入 URL 后，请求到响应之间会经过哪些协议和流程。
    2. HTTP 协议请求格式包含哪些部分
    3. HTTPS / TLS 的混合加密和密钥协商大致流程是什么。
    4. TCP 三次握手和四次挥手的流程是什么，各阶段验证了什么能力。
    5. TCP 和 UDP 的区别是什么，分别适合什么使用场景。
    7. SSE 和 WebSocket 有什么区别，项目里为什么使用 SSE。
3. 大文件上传与 NAS 使用场景
    1. 如果需要设计分片上传，应该如何设计。
    2. 用户取消上传如何处理。
    3. 断点续传如何做
4. Gin 请求流程、中间件与缓存
    1. 一次请求进入 Gin 框架后的完整处理流程是什么。
    2. Gin 的 context 和标准库 context 有什么区别。
5. Docker 文件挂载可以使用哪些参数，volume 和 mount 有什么区别。
7. Elasticsearch 到 Bleve、中文分词与资源配置
    1. CJDict 分词查找为什么会带来内存和 CPU 问题，如何用前缀树和扁平数组优化。
    2. gRPC 是否了解，它适合什么服务调用场景。