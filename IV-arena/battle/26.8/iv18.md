[问题]
1. Go Map
    1. Map 改成 Swiss Table 结构改变的原因是什么
    2. 写多的时候 sync.Map 为什么性能差
2. 逃逸分析
    1. 逃逸分析是怎么做的、有哪些逃逸情况
    2. 写业务代码时会不会刻意规避逃逸，还是认为逃逸是合理的
3. InnoDB
    1. 介绍 InnoDB 存储引擎，以及为什么 MySQL 要用它作为默认存储引擎
    2. 三层 B+ 树大约能存多少数据，2000 万是怎么算出来的；一页大概有多大
    3. 事务里的一致性是怎么保证的
    4. 事务已提交但只写在日志、未落盘时机器宕机，重启后这条数据会丢吗；恢复流程是什么样的
4. Redis
    1. 缓存穿透、击穿、雪崩分别是什么（第一种把穿透说成了雪崩）
    2. 有序集合为什么用跳表而不是红黑树这类树状结构
6. 网络
    1. DNAT 是什么意思

sync.Map、InnoDB存储引擎、崩溃后恢复、zset跳表、SNAT和DNAT