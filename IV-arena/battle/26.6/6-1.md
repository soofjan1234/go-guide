[问题]
1. 复杂项目
2. 文档中心 / Bleve
    1. 看了源码、调了参数之后，具体是什么原理导致内存占用降低
    2. 智能文档是否需要切块后再建索引；切出来的块本身是什么结构，是纯文字吗
    3. 整个文档系统存储量级大概有多大（多少条）
    4. 用户侧出现慢查询/慢操作时，会不会把这部分信息上报
3. Go 基础
    1. 切片和map是线程安全的吗
    2. GMP 调度器里 P 是什么
    3. 程序启动时会启动多少个 P；和 CPU 核数是什么关系，1:1 吗
    4. 如何控制「始终保持 10 个并发」；除了 Channel 还有什么方式
4. MySQL / 查询优化
    1. MySQL 里接触过的、数据量最大的表是什么；大概什么量级（万级/百万级/亿级）
    2. 有没有做过查询优化
5. Redis / 缓存
    1. Redis 缓存创建和删除的时机分别是什么；双写一致性
    2. 活动类等热点数据，Redis 也会被打满，还有什么方案缓解 Redis 压力
6. NSQ
    1. 消息丢失和重复消费分别怎么处理
    2. 需要保证消息顺序消费时，NSQ 是否支持
7. Cursor / AI 开发
    1. 一个需求过来，如何用 Cursor 完成编码和自测
    2. 哪些 code 是 AI 做的，哪些是人自己做的
    3. 会不会包括规范类、约定俗成的规则（如 rules）
    4. AI 生成代码第一遍能否完全满足需求
    5. 不对的话是人改还是让 AI 继续改
    6. 有哪些 skill 应用在整个开发过程中
    7. 怎么确定问 AI 时用到了这些 skill

文档中心、MySQL、缓存、AI