[问题]
1. Go 代码题
    1. func f() {
            go func() {
                panic("panic in goroutine")
            }()

            time.Sleep(1 * time.Second)
            fmt.Println("after sleep")
        }
    2. func f() {
            defer func() {
                if r := recover(); r != nil {
                    fmt.Println("Recovered in f", r)
                }
            }()

            go func() {
                panic("panic in goroutine")
            }

            time.Sleep(1 * time.Second)
            fmt.Println("after sleep")
        }
    3. func f() {
            gnum := 0
            wg := sync.WaitGroup{}
            count := 10000

            wg.Add(1)
            for i := 0; i < count; i++ {
                go func() {
                    defer wg.Done()
                    
                    mutex.Lock()
                    gnum++
                    mutex.Unlock()
                }()
            }
            wg.Wait()
            fmt.Println(gnum)
        }
    
    4. 如何向初学者解释这段waitgroup代码的问题？
2. 机器状态包括在线、离线、下线中、维护
    1. 某台机器被人工设置成维护状态后，过几秒又变成在线状态；
        重启状态管理服务后，机器短暂消失，过一会又恢复。
        请分析可能原因和定位思路。你也可以提问，提好问题对回答很重要。
    2. 说出猜想后，你怎么验证？
    3. 还有别的可能吗
    4. 心跳返回在线，我们手动设置维护，过一会又被心跳改回在线。
        可能只有你一个人在操作，也不一定是两个人竞争。可能有什么原因
3. 重复任务下发
    1. 同一台机器被重复分配任务，可能是什么原因？
    2. 怎么验证你的猜想？
4. 经验追问
    1. 是否做过监控？
    2. 是否了解 SRE？
    3. 是否有高并发、同步相关经验？
5. 热门商品库存扣减
    1. 1000 件热门商品、10 万人同时购买，如何设计后端扣减方案？
    2. 如何保证不能超卖，同时尽量提高吞吐？
11. 官网访问慢排查
    1. 用户反馈官网访问慢，如何排查？
    2. 如果确定接口慢，如何排查？
    3. 除了F12，还有什么
12. 两根长短不一的蜡烛，都是1小时燃烧完，如何计算燃烧了45分钟

