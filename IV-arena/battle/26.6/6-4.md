[问题]
1. 最近项目 / NAS 文档中心
    1. 最近工作的最新项目是什么；是否只有 NAS 相关项目
    2. 遇到的棘手问题
    3. NAS 项目整体技术架构；请求如何进入（网关 5080 端口、路由分发）
    4. 设备硬件规格（CPU 核数等）
2. 限流
    1. 限流算法了解哪些；项目里用的什么
    2. Token Bucket 实现方式（golang.org/x/time/rate）；桶内算法原理
3. 运维
    1. Docker From的目的
    2. K8s 熟悉程度
    3. Nginx 熟悉到什么程度
4. MySQL 
    1.  B +树好处
    1. ACID 四个特性分别是什么
    2. 四种隔离级别（读未提交、读已提交、可重复读、串行化）
5. 缓存穿透 / 击穿 / 雪崩
    1. 缓存穿透是什么；怎么解决（无效 key 缓存、限流、布隆过滤器）
    2. 缓存击穿是什么；怎么解决（永不过期、提前预热）
    3. 缓存雪崩是什么；怎么解决（集群、分散失效）
6. 网络
    1. HTTPS
    2. 跨域 
7. NSQ 消息队列
    1. 使用消息队列需要关注什么（重复消费、消息丢失）
    2. 重复消费怎么防（消息 ID + 主键去重）
    3. 消息丢失有哪几种情况；怎么解决（持久化、ACK 时机）
    4. NSQ 与 Kafka 对比；各自适用场景
8. 前后端分离 / 容器打包（场景题）
    1. 前后端分离项目如何用 Docker 打包部署
    2. 前端用什么构建；产物是什么（dist / public）
    3. 后端 Dockerfile 怎么写（Alpine 最小镜像）
9. Go 
    1. G、M、P 分别是什么；Scheduler 全局调度中心
    2. CSP「不要共享内存，用通信来通信」怎么理解
    3. Channel 内部结构（缓冲区、等待队列、select）
    4. 从已关闭的 Channel 还能读到数据吗；读完后继续读会怎样
    5. GC 触发方式有哪几种（堆指针阈值、定时、手动）


限流、运维、nignx、ACID、缓存三兄弟、跨域、https、分布式锁、skills
