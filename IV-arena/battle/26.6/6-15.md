[问题]
1. AI 工具使用（Cursor / Codex）
    1. 主要使用什么 AI 工具？Cursor 和 Codex 具体怎么用、怎么分工
    2. Cursor 是自己买的吗？公司对 AI 开发有什么要求
    3. 常用什么模型（GPT、Claude）？什么情况选哪个
    4. Cursor Pro 版本？会不会遇到限流、切 Auto？有没有办法减少限流用量
    5. 是否用过 Cursor Plan 模式；写方案放外部免费版、写代码用 Cursor 的分工是否合理
    6. 技术方案是什么形式（流程文档）？外部 GPT 不知道项目上下文，选型偏 Java/Go 怎么办
    7. 方案做好后如何生成代码；是否逐块 accept/commit
    8. TDD 流程：测试用例在写代码之前还是之后写
    9. Cursor Rules / Skills 有没有用；团队代码规范文档放在哪、如何维护；为什么不放进项目 .cursor
    10. Codex 个人项目分享；网站部署在哪；Codex 跑在开发还是生产环境
    11. Codex 带来哪些印象深刻的效率提升；个人网站实现了哪些功能
2. 云端
    1. 穿透
        1. 流程讲解
        2. NAT 四种类型是否了解；对称型 NAT 与打洞/中转的区别；延迟与成功率
    2. NSQ 消息队列（云端）
        1. 引入前是否同步写库；为什么用 NSQ；峰值 QPS 多少；吞吐提升如何测（P95/P99）
        2. 消息丢失有哪几种情况；如何预防（持久化、ACK）
        3. 重复消费如何处理（消息 ID + 去重）
        4. NSQ 持久化机制；ring buffer 在内存还是磁盘；宕机消息是否全丢
        5. NSQ 与业务服务是否同进程；重启关系；几个 consumer
        6. 为什么选 NSQ 而非 Kafka/RocketMQ/RabbitMQ；消息丢了业务能否接受；最终一致性如何保证
        7. Topic/Channel 模型；一条消息是否广播给所有 consumer
        8. NSQ 是你主动引入还是老大安排
    3. 应用层加密（云端）
        1. AES+RSA 混合加密为了什么；请求链路 VPN/TLS 中间人
        2. TLS 证书谁颁发；是否存在两套 TLS；中间人解密场景
        3. AES 和 RSA 分别用在哪个阶段；各自优缺点
6. 文档中心 / Bleve
    1. 为什么从 ES 换成 Bleve；8GB NAS 内存；ES 占 2GB、Bleve 初始化约 100KB
    2. Bleve 与 ES 区别；是否都是文档型；Bleve 倒排索引如何在磁盘/内存维护（segment、合并）
    3. Bleve 与 ES 内存占用差异原因；建索引时内存飙高问题
    4. 分块索引方案如何工作（主文档/子文档、Map 聚合）
    5. 中文分词用的什么；Bleve 默认分词对中文效果；是否测过
    6. GPT 全文检索与多粒度检索：用户端改写后去 Bleve 查
7. 文件索引同步器
    1. BFS+Queue 如何替代 fsnotify；目录遍历与异步处理
    2. 实时性变弱能否接受；有没有做过优化（批量、read 限制 1000、跳过未修改文件）
8. 相册 AI / DBSCAN
    1. RK 平台、NPU、JNI/Kotlin 封装是什么
    2. DBSCAN 算法原理；与 K-Means 区别；参数（半径、最小点数）
    3. 算法是你实现的吗；为什么选 DBSCAN；是否 AI 生成方案
    4. 97.3% 准确率怎么得出；测试集是否公正、有无偏见
9. 反问
    1. 项目是不是了解不太够（面试官未直接评价）
    2. 公司在做什么项目/技术栈

