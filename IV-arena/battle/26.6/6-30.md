[问题]
1. 自我介绍 / 技术优势
    1. 自我介绍中如何概括自己的专业能力、性能调优能力、跨端协作能力和 AI 工作流能力
    2. 文档中心内存优化、云端穿透、AI 辅助开发分别如何体现能力
2. 文档中心 / Bleve 内存优化
    1. 文档中心上线初期用户反馈执行异常，如何确认现象、复现问题并定位到 Bleve 建索引内存高
    2. 如何通过 top、pprof 和源码阅读定位问题
    3. Bleve 中 all 字段 / `_all` 索引的作用是什么；为什么关闭它能降低 segment 体积
    4. `_all` 索引为什么存在；相比分别查 title/content，它的设计收益是什么
    5. Bleve 底层倒排索引大概如何存储词项和文档位置
    6. title 和 content 分开建索引与合并索引，在空间上可能有什么差异
    7. 为什么在 NAS 环境里从 Elasticsearch 换成 Bleve
3. 分块索引 / 主文档与子文档
    1. 大文档为什么要分块；主文档和子文档是什么关系
    2. 搜索结果中主文档和子文档如何归并、去重和拼接
    3. 分块时是否可能把词语切断；如何选择切分点
    4. title/content 是否分词；其他字段是否分词
    5. Bleve 匹配关键词的过程如何举例说明
    6. mem segment / segment 相关概念是否了解
4. 文档类型识别 / MIME Type
    1. 如何判断一个文件是否真的是可解析文档，而不是只看后缀
    2. 后缀名、MIME Type、Office 内部 content type XML 分别如何参与判断
    3. 支持的文档类型有哪些：PDF、doc/docx、PPT、txt 等
    4. 如果 Word/Visual 文档改名为 txt 仍能打开，索引器应该如何处理
    5. 面对大量奇怪格式和后缀，如何设计可扩展的解析策略和测试集
5. AI 相册
    1. AI 相册比较困难的点是什么：RKNN / ARM NAS / cgo 调用 native 模型 / 调参
    2. 模糊照片检测如何通过拉普拉斯方差判断
    3. 图片缩放到固定尺寸后再检测的原因；尺寸如何选择
    4. 相似照片检测中 pHash / 视觉哈希 / 汉明距离如何工作
6. 文件同步 / fsnotify / BFS 队列
    1. 文件索引同步要解决什么：数据库索引与真实文件系统一致
    2. fsnotify 的问题：每个目录 watcher 带来内存开销，文件多和操作多时可能漏事件
    3. 为什么从实时监听改为广度优先遍历 + 队列
    4. BFS 同步流程：目录入队、子文件处理、子目录继续入队
    5. 同步触发时机：用户进入网盘或点击同步按钮
    6. 删除文件后数据库记录如何清理；展示时校验真实文件是否存在并异步删除数据库路径
7. 反问
    1. 对 Bleve 层面关键词搜索还有哪些不理解
    2. 公司目前在做什么产品

文档中心、Bleve、倒排索引、分块索引、MIME Type、AI 相册、拉普拉斯方差、pHash、fsnotify、BFS


[同类追问预测]

这个面试官的风格是围绕项目细节连续下钻：先问你做了什么，再追“为什么这么设计、底层是什么、边界情况怎么办、换一种异常输入会不会出问题”。如果还是这个人来面，可能继续问：

1. 文档中心 / Bleve 内存优化
    1. 你说关闭 `_all` 能减少一份索引，那如果用户需要全字段搜索，你怎么保证召回效果不下降？
    2. 如果不用 `_all`，你会怎么构造 title、content 多字段查询？不同字段权重要不要区分？
    3. Bleve 建索引时内存高，到底高在什么对象上？是 segment、batch、posting list，还是文档内容暂存？
    4. pprof 里你看到的热点函数是什么？怎么从热点函数推到关闭 `_all` 这个方案？
    5. 关闭 `_all` 后有没有做过搜索准确率和性能对比？用什么数据集测？
    6. 如果一个文档有 title、content、tag、author 多个字段，哪些字段应该索引，哪些字段不应该索引？
2. 倒排索引 / Segment
    1. 倒排索引里的 posting list 里面通常存哪些信息？
    2. 为什么倒排索引适合关键词搜索，但不适合模糊语义搜索？
    3. segment 为什么通常是不可变的？不可变带来什么好处和代价？
    4. 多个 segment 查询时怎么合并结果？segment 太多会有什么问题？
    5. segment merge 会不会造成 CPU、IO 或内存抖动？在 NAS 上怎么控制？
    6. 如果一个词在很多文档里都出现，posting list 很长，查询性能怎么优化？
3. 分块索引 / 主文档与子文档
    1. 大文档按固定大小分块时，关键词刚好跨块边界怎么办？
    2. 子文档命中后，为什么要归并到主文档？如果多个子文档都命中，排序分数怎么合并？
    3. 分块大小怎么确定？太大和太小分别有什么问题？
    4. 子文档和主文档在 Bleve 里是同一个 index 还是不同 index？字段怎么设计？
    5. 删除主文档时，如何保证所有子文档也被删除？
    6. 更新大文档的一小段内容时，是全量重建索引还是局部更新？为什么？
4. 文档类型识别 / MIME Type
    1. 只看后缀为什么不可靠？只看 MIME Type 又有什么问题？
    2. 如果一个 docx 文件被改成 txt 后缀，但用户仍希望能搜索，你会按什么策略解析？
    3. Office 文件为什么要看 `[Content_Types].xml`？它能解决哪些误判？
    4. 如果 MIME Type 检测出来是 `application/octet-stream`，你会怎么兜底？
    5. 解析器失败后，是直接跳过、重试、降级为纯文本，还是记录错误给用户？
    6. 如果一个文件很大或者解析很慢，怎么防止它拖垮整个索引任务？
5. AI 相册 / 模糊照片 / 相似照片
    1. 拉普拉斯方差为什么能判断模糊？它对噪声、压缩、低光照片敏不敏感？
    2. 图片缩放到固定尺寸后再算方差，尺寸怎么选？不同尺寸会不会影响阈值？
    3. 阈值是怎么定出来的？有没有标注集、误判率、召回率这些指标？
    4. pHash 和普通 hash 有什么区别？为什么 pHash 能判断相似图片？
    5. 汉明距离阈值怎么定？如果两张图裁剪过、旋转过、加滤镜，还能识别吗？
    6. AI 模型跑在 RKNN / ARM NAS 上，模型加载、推理并发、内存占用怎么控制？
6. 文件同步 / fsnotify / BFS 队列
    1. fsnotify 漏事件你是怎么复现的？文件多、目录深、操作频繁分别会有什么表现？
    2. 从实时监听改成 BFS 扫描后，实时性下降用户能不能接受？产品上怎么解释？
    3. BFS 扫描大目录时，怎么限制 IO、CPU、内存和数据库写入压力？
    4. 扫描过程中用户同时新增、删除、移动文件，数据库和真实文件系统怎么做到最终一致？
    5. 如果用户反复进入网盘触发同步，会不会重复扫描？怎么做任务去重和频控？
    6. 删除文件后数据库记录异步清理，如果清理失败怎么办？下次如何兜底？
7. 项目表达 / 深挖套路
    1. 你说“内存下降 70%”，这个数字怎么测出来？测试数据、机器规格、指标口径是什么？
    2. 你说“部署简单”，具体简单在哪里？二进制大小、依赖、启动速度、资源占用怎么对比？
    3. 你说“性能不错”，是写入性能、查询性能，还是资源占用表现不错？
    4. 如果让你重新做这个文档中心，你会保留哪些设计，改掉哪些设计？
    5. 这个项目里有没有线上事故或用户投诉？你怎么定位、止血和复盘？
