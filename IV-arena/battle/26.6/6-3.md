[问题]
1. NAS相对于友商的优点
2. fsnotify → BFS + 队列（文件索引同步）
    1. 为什么 fsnotify 不适合海量文件场景
    2. 队列里存的是什么
    3. 怎么控制队列大小
    4. 服务重启后任务怎么处理；状态机
    5. 新增 / 修改 / 删除文件怎么被发现；真实文件 vs 数据库对比
    6. 任务是否一直在跑；执行完就停；什么时机触发重新扫描
    7. 未修改文件 / 文件夹怎么跳过
4. 内存优化（文档中心 / Bleve）
    1. 内存占用怎么检测；用什么指标
    2. 500MB vs 1GB 文件对比实验怎么做
5. 限流 / 缓存（Ristretto）
    1. Token Bucket 限流；NAS 场景 TPS 大概多少
    2. 限流主要防什么（恶意重复点击）
    3. 缓存 Key 怎么设计（接口 + 请求内容 + Token 识别用户）
    4. 缓存存什么（响应信息）；TTL 怎么设（1 分钟）
    5. 有没有缓存击穿；缓存命中率多少（压测 ~80%）
    6. 为什么不用 sync.Map，而用 Ristretto
6. 广告出价接口设计（场景题）
    1. 万级 QPS 流量；100ms 内返回出价结果；怎么设计接口
    2. 日志要不要同步落库；更好的日志处理方式
    3. 超时控制怎么做（Context 100ms deadline；Controller/Service 层传递 Cancel）
    4. Goroutine 生命周期在超时场景下怎么走
    5. 有没有遇到过 Goroutine 泄漏
7. 频控（场景题）
    1. 每个用户每条广告 1 分钟内最多曝光 3 次；怎么实现
    2. Redis 用什么数据结构；Hash 怎么设计 Key/Field
8. MySQL / 查询优化（场景题）
    1. 百万级广告活动表；按地域 + 时间找可投广告；怎么建表 / 优化
    2. 每次请求都查表是否合理
9. 其它
    1. K8s / Docker 用得多少；生产是否直接部署到机器
    2. 有没有遇到过较严重的线上事故

NSQ、文件同步、限流缓存、广告出价、频控、MySQL
