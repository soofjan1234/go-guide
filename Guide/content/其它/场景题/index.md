---
title: 场景题
weight: 7
date: 2026-06-09
draft: false
---
---

# 高并发

## 广告出价接口设计 +1

**背景**：服务收到万级 QPS；收到请求后需解析、过滤、打分、出价，**100ms 内**返回 Response；还要收集日志（什么流量、是否参与出价、打分多少）。

**主问**：
1. 如果是你，怎么设计这个接口？
2. 怎么确定一定是 100ms 以内返回？接口超时控制怎么做？
3. 日志是落库吗？你觉得日志要不要**同步**落库？除消息队列外还有什么更好的处理方式？
4. Goroutine 的生命周期在这个超时场景下怎么走？有没有遇到过 Goroutine 泄漏？

---

## 热门商品库存扣减 +1

**背景**：1000 件热门商品、10 万人同时购买。

**主问**：
1. 如何设计后端扣减方案？
2. 如何保证不能超卖，同时尽量提高吞吐？

---

## 短视频点赞系统 +1

**背景**：你负责一个短视频平台的点赞系统。某条视频突然爆火，5 分钟内收到 1000 万次点赞请求，峰值 20 万 QPS。用户点完赞后，需要立刻看到“已点赞”状态，视频详情页的点赞数也要尽快变化，同时系统还要更新作者通知、排行榜、推荐特征。

**主问**：

1. 这个场景下会出现哪些问题？你会如何处理？
2. 还有个浏览历史的功能，你会怎么设计？

### 热点 Key / 大 Key 问题

**主问**：
1. 某条视频/某个明星/热门商品突然爆火，Redis 单 key 被打爆怎么办？
2. 热点 key 如何发现？

# 业务设计

## 广告频控 +1

**背景**：对每个用户在**每一条广告**下做频控——最近 **1 分钟最多曝光 3 次**。

**主问**：
1. 你会怎么实现？
2. Redis 用什么数据结构？Key / Field 怎么设计？

---

## 连续签到 7 天奖励 +1

**背景**：用户连续签到 7 天要给奖励。

**主问**：你会怎么设计？

---

## 独立访客（UV）计数 +1

**背景**：统计独立访客数量。

**主问**：你会怎么实现？（考点常指向 HyperLogLog / Bitmap 等）

---

## 重复请求 / 防抖去重 +1

**背景**：第一个请求发出后，抖动还没结束又发了第二个请求。

**主问**：如何做去重，保证不重复操作？

---

# 现实问题

先定影响面，再看变更；沿链路查监控、日志、资源和依赖；必要时先止血，再复现修复。

## 接口突然大量超时如何排查？

先看是不是全量超时；
再看链路：服务、DB、Redis、MQ、第三方；
再看资源：CPU、内存、网络、连接池；
最后止血：回滚、限流、降级、扩容，用 pprof/trace 定位。

## 上线后发现问题怎么排查？网页、app两种情况

影响面 + 版本变更 + 前端/APP/后端三层排查 + 回滚复现

## OOM怎么排查？

趋势 → 谁被杀 → heap → goroutine → 代码泄漏点  → 先保现场

## 官网访问慢排查

用户范围 → CDN → DNS/TCP/SSL → 服务端 → 前端资源 → 链路丢包

## CPU\内存高怎么排查？范围在MySQL的话呢？

通用排查：
1. CPU 高：用 top/htop 看是哪个进程占 CPU，Go 服务抓 CPU profile 看热点函数。
2. 内存高：看内存使用趋势，Go 服务抓 heap profile 看存活对象。
3. 看日志：是否有死循环、正则/JSON 热点、大量 GC、锁竞争。

MySQL 范围：
1. 看 processlist：是否有长时间运行的 SQL、锁等待。
2. 看慢查询日志：定位具体慢 SQL，用 EXPLAIN 看执行计划。
3. 看索引：是否有索引失效、缺失索引。
4. 看连接数：是否连接数打满，活跃连接是否过多。
5. 看系统资源：MySQL 所在机器的 CPU、磁盘 I/O、内存是否瓶颈。
6. 看锁：是否有表锁、行锁等待，事务是否过长。

## 一部分用户出现下单失败的问题，让你去定位这个线上事故

先止血 → 收集用户特征 → request_id 查链路 → 看错误码 → 看数据特征 →  复现修复

## 慢查询怎么办

1. 确认瓶颈是不是数据库。
2. 定位具体哪条 SQL 慢。
3. 用 EXPLAIN / EXPLAIN ANALYZE 查看执行计划，重点关注 type、key、rows、Extra。
4. 检查索引是否命中，有没有索引失效。
5. 分析 SQL 本身，是否存在 SELECT *、返回数据过多、复杂 JOIN、排序或分组等问题。
6. 检查是否存在锁等待，事务过长。
7. 检查系统资源，例如 CPU、磁盘 I/O、内存是否成为瓶颈。
8. 优化后再次验证，确保确实改善。


先定影响面，看变更，沿链路看观测，业务层，止血
