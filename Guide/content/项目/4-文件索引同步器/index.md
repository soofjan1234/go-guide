---
title: 文件索引同步器
weight: 4
date: 2026-06-12
draft: false
---

## 文件索引同步的作用是什么

同步文件和数据库里的文件索引

本来是在网盘项目里实现这个功能，然后其它项目调用它，但是像相册、音乐都有自己需要的一些文件特性，比如exif、id3等，会使得网盘存储一些不需要的属性

所以加个中间层，文件索引同步器对文件进行同步，网盘、相册、音乐来使用文件索引同步器更新数据库，做到了单一职责以及解耦

# BFS+Queue
## 为什么不用fsnotify

这个同步方案我们做过取舍，最终没走纯实时监听，而是用了用户触发同步 + 定时兜底。
原因是 fsnotify 在目录规模上来后 watcher 数量多、内存压力大，而且有漏事件，稳定性不够。
我们改成先做目录快照比对，再把新增/修改/删除投递到队列，worker 批量写库。
这样实时性弱一点，但准确性和资源可控性更好，整体更适合我们的场景

## fsnotify的原理

1. fsnotify 创建一个 inotify 实例（对应一个 fd），对某个目录或文件 add watch
2. 内核记录这个路径上发生的变化，通知这个 inotify 实例，把事件写到对应 fd 的事件队列
4. fsnotify 从 fd 里读事件，转成 Go 里的 Event

## 为什么是BFS而不是DFS

1. **用户体验**：BFS 先同步浅层目录，用户常用的文件夹能更快被索引到
2. **代码简洁：**Go 的 channel 本身就是 FIFO 队列，天然适合 BFS，代码也更简洁。DFS 如果递归扫到底，一个任务可能非常长，深层目录还可能带来调用栈和长事务问题

## Queue 是内存队列还是数据库队列？

是两者结合。内存 channel 负责运行时调度，数据库任务表负责持久化状态。

新任务会先写入任务表，状态是 Pending，然后再投递到内存队列。worker 处理前把任务改成 InProgress，处理完成后删除或标记完成。这样服务重启后可以从数据库恢复未完成任务。

## 自研 BFS + Queue 替代 fsnotify 全量监听，海量文件场景内存开销降低 90%

通过不同的目录树，比如占用500-1-2GB，使用go runtime.MemStats查看内存峰值的开销，对比内存平均开销降低 `90%+`。

## 百万级文件如何控制内存？如何优化？

1. 多goroutine处理吞吐加快60%
2. 扁平 100 万 / 单目录：优化前 RSS 1392MB → 流式读后 41MB
3. 分层 100 万 / 1000 目录：仅 BFS 队列仍 118MB heap、1 天 → 加批量提交后 9.4MB、38 分钟

# 任务机
## 任务状态机有哪些状态？

核心状态是 Pending 和 InProgress。

Pending 表示任务已提交但还没处理；worker 开始处理前把它改成 InProgress；处理完成后删除任务或标记为完成。失败时可以重新回到 Pending，等待后续重试。

## 服务崩溃时正在处理的任务怎么办？

崩溃时任务可能停留在 InProgress。服务启动后会扫描任务表，把残留的 InProgress 任务重置成 Pending，然后重新投递到队列里。

因为目录扫描任务设计成幂等的，重复执行同一个目录不会造成错误。已经处理过且没有变化的文件会被比较逻辑跳过。

# 可能延伸追问

## BFS、扫描和对比

1. 目录快照比对具体比哪些字段？path、size、mtime、inode、hash 都用吗？
2. 只用 mtime 和 size 会不会漏掉内容变化？
3. 文件重命名如何识别？是删除+新增，还是能保留同一个 fileId？
4. 文件移动目录时，索引如何更新？子目录下大量文件移动会不会很慢？
5. BFS 扫描时遇到软链接、循环链接、权限不足目录怎么处理？
6. 单目录百万文件时，为什么普通读目录会占 1GB 内存？流式读具体怎么做？
7. BFS 队列本身会不会堆积太多目录？怎么限制队列长度？
8. 定时兜底的周期怎么定？实时性和资源开销如何权衡？
9. 扫描过程中用户继续增删改文件，会不会出现快照不一致？
10. 如果扫描一个大目录耗时很长，中途再次触发同步怎么合并任务？

## Queue 和任务状态机

1. 为什么核心状态只有 Pending 和 InProgress？需不需要 Done、Failed、Retrying？
2. InProgress 重置为 Pending 时，如何判断它是真的崩溃遗留，而不是还有 worker 在跑？
3. 多实例部署时，两个 worker 如何避免抢同一个任务？
4. 数据库任务表如何加锁？用乐观锁、状态 CAS，还是事务 select for update？
5. 任务失败重试有没有次数上限？一直失败的目录怎么处理？
6. 任务粒度是目录、文件，还是批次？粒度太粗和太细分别有什么问题？
7. 内存 channel 和数据库任务表之间，如果写 DB 成功但投递 channel 失败怎么办？
8. 如果投递 channel 成功但 worker 还没改 InProgress，服务崩溃会怎样？
9. 任务队列如何做优先级？用户正在看的目录是否应该优先同步？
10. 如何观测任务队列健康度？看 pending 数、处理耗时、失败率还是扫描速度？

## 百万文件性能

1. 多 goroutine 吞吐提升 60%，瓶颈从哪里转移到了哪里？
2. 批量提交为什么能把 1 天降到 38 分钟？主要减少了什么成本？
3. 批量大小怎么定？太大和太小分别有什么问题？
4. 100 万文件写库时，如何避免 SQLite/MySQL 锁竞争？
5. 内存降低 90% 的测试场景是什么？目录深度、文件名长度、机器配置是什么？
6. RSS、heap、对象数量分别怎么测？为什么只看 MemStats 可能不够？
7. 扫描时如何避免一次性把目录项全部加载到内存？
8. 大目录排序会不会额外占内存？如果用户希望按名称展示，排序在哪里做？
9. 如果文件系统本身很慢，比如机械硬盘或网络盘，worker 数怎么调？
10. 百万文件首次全量后，后续增量如何保证不会再退化成全量？

## 一致性和异常

1. 文件正在写入时被扫描到，如何避免索引半个文件？
2. 如何判断文件已经稳定？会不会延迟一段时间再处理？
3. 数据库写成功但业务回调失败，整体状态怎么处理？
4. 文件删除后，业务侧相册/音乐的派生数据怎么清理？
5. 索引表和真实文件系统不一致时，有没有修复任务？
6. 如果用户手动改系统时间，基于 mtime 的增量扫描会不会出错？
7. 权限不足的目录是跳过、记录失败，还是持续重试？
8. 扫描任务如何支持取消？取消后已经写入的部分怎么办？
9. 如果服务升级导致任务表 schema 变化，旧任务如何兼容？
10. 幂等具体靠什么保证？唯一键、版本号、文件指纹，还是 upsert？
