---
title: 邮件系统1
weight: 40
date: 2026-06-12
draft: false
---

# 项目背景

这是一个面向 NAS 售后的邮件智能助手，结合知识检索与人工审核，为客服生成可追溯、可控风险的英文回复草稿。

## 主要流程

新邮件进入工作流后：
1. 先做分类和风险判断
2. 再分别检索产品文档和已审核案例，经过全文检索、向量检索和 RRF 融合。
3. 模型输出的分类、引用和草稿还会经过后端校验；证据不足、高风险或疑似提示注入时直接转人工。
4. 草稿生成后，LangGraph 在 interrupt() 节点暂停，审核人可以批准、编辑后批准、拒绝或转人工
5. 发送

## 为什么要 Agent 而不是 固定流程+RAG

依然是固定流程+RAG的主体，只不过Agent的作用的语义不确定时做什么：

例如先判断邮件类别和风险；检索后判断证据是否足够；不足时是否改写查询再检索；有依据后才生成草稿。

用有限状态图限制它的动作。

# 数字

## 实际处理多少邮件？主要是什么售后问题？

处理了2千多封邮件

主要是 NAS 产品的系统软件、硬件和使用支持问题：
- 系统与固件：下载、升级、服务管理、Docker 功能。
- 设备使用：开机界面、网络连接、系统重建、数据迁移。
- 硬件与配件：硬盘稳定性、风扇噪音、触控笔配件。


# 技术选型

## 技术

项目主要在模型调用、业务迭代，吞吐并不高，Python 的 AI 生态和实验效率更适合当前 MVP

## Http 框架

1. FastAPI 基于 ASGI，适合异步 HTTP 接口：邮件系统有大量外部IO，比如调用模型网关、查询PostgreSQL
2. FastAPI 是直接把 Pydantic 作为“底层基石”来构建的，可以统一处理参数校验

**其它**

1. Flask 更轻，但项目需要类型校验、OpenAPI、异步接口和较多业务契约。如果用 Flask，这些能力通常需要额外组合多个扩展；FastAPI 在这些方面提供了相对统一的默认方案
2. Django 的后台管理、ORM很成熟，对于当前项目可能稍重

## 数据库

PostgreSQL 同时承载：

- 邮件、审核、候选案例和审计等业务数据；
- LangGraph checkpoint、interrupt 和恢复所需的运行状态；
- 使用 `tsvector` 和 GIN 的关键词检索；
- 使用 pgvector 的向量语义检索。

选择 pgvector 的原因：业务数据本就需要 PostgreSQL，将向量存入同一数据库可以减少独立向量数据库、跨库同步和一致性处理

## Embedding

对比了 Qwen3 0.6B、Nomic v1.5、Snowflake Arctic M v1.5 和 BGE-M3 

1. 覆盖真实场景：先拉取邮件，选300+，覆盖分类也是方方面面的
2. 产品文档和已审核案例分别统计 Recall@1/3、MRR；型号和错误码必须设为 Top-3 硬门槛。
3. Snowflake 在扩展测试集上的两类检索 Recall@3 均达到 100%，查询延迟 P95 约 58 毫秒，速度和内存占用也是本轮表现最好的

补充：
- Recall@K（召回率@K）：检索出的前 K 个结果中，有没有包含标准答案（Ground Truth）
- MRR（Mean Reciprocal Rank，平均倒数排名）：把所有 Query 中正确答案第一次出现的排名的倒数求平均
- 型号和错误码必须设为 Top-3 硬门槛：因为这是向量模型的天然弱点——像版本号、错误码这种一字之差但业务致命的 Query，稠密向量很容易打高分误召回

## Agent 框架

1. 本项目选择 LangGraph，主要因为需要展示非线性的高级 RAG 工作流：查询改写、证据评估、生成校验、有限重试、多分支路由和人工兜底。
2. LangGraph 可以通过 Conditional Edges 和 Cycles 显式表达这些路径，并通过 Checkpoint 和 Interrupt 保存、暂停及恢复工作流。  
3. 它替代的是自研 Agent 流程状态机，不替代邮件、审核、知识库等业务表，也不替代权限校验、审计记录和副作用幂等机制。

成本：
1. 要定义稳定 State、维护 PostgreSQL Checkpointer、处理节点重放和并发恢复，排查链路比普通同步函数复杂。
2. 所以我只把它用于这类有状态、有分支、有人工兜底的流程。


## 搜索

1. 向量搜索：能理解同义表达和语义，例如客户说“共享文件夹访问失败”也可能召回 SMB 故障文档
2. BM25 / 关键词检索：精确术语、型号、错误码命中稳定，可解释、成本低

### BM25

1. IDF：全局越罕见的词，权重越高。比如搜 the cat，the 在 99% 的文档都出现，其得分贡献几乎为 0；而 cat 权重极高。
2. TF 饱和度：在传统 TF 中，词出现 10 次得分是 1 次的 10 倍。BM25 认为这是不合理的，一个词在一篇文章里出现 5 次后，出现第 6 次带来的边际效益急剧递减并趋于饱和。
3. 文档长度归一化：长文章天生容易包含更多词，BM25 会惩罚长文章，避免大长篇依靠无意义的堆砌词排在前面。

### 有rerank吗

目前没有 Rerank 环节。当前流程是：全文关键词检索和向量检索各取候选，再用 RRF 融合排序，每类知识最多保留 3 条。

设计上把 Cross-Encoder 重排放到第二版：只有当 Recall@K 已经达标、但前几名排序仍持续不准时，才引入它做候选精排，避免在 MVP 阶段额外增加推理延迟和部署复杂度。